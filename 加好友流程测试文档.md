# 加好友流程测试文档

## 🎯 功能概述
实现了完整的"先加好友再发消息"功能，包括：
1. 发送好友请求（preview.js）
2. 接收和处理好友请求（chat.js）
3. 接受/拒绝好友请求（chat.js）
4. 实时监听好友状态变化

## 📱 测试流程

### 1. 发送好友请求测试
**位置**: `pages/preview/preview.js` - `addFriend` 方法

**测试步骤**:
1. 进入任意用户的名片页面（preview页面）
2. 点击"打招呼"按钮
3. 输入消息内容
4. 点击"发送"按钮

**预期结果**:
- 如果已经是好友：直接跳转到聊天页面
- 如果需要验证：显示"好友申请已发送，请等待对方同意"
- 如果发生错误：根据错误码显示相应提示信息

**关键代码**:
```javascript
// 使用双向好友类型
let promise = wx.$TUIKit.addFriend({
  to: targetUser.userID,
  type: wx.TencentCloudChat.TYPES.SNS_ADD_TYPE_BOTH,
  remark: '备注名',
  wording: message
});
```

### 2. 接收好友请求测试
**位置**: `pages/chat/chat.js` - `loadFriendRequests` 方法

**测试步骤**:
1. 进入聊天页面
2. 查看是否有新的好友请求
3. 检查控制台日志

**预期结果**:
- 页面显示待处理的好友请求列表
- 控制台输出好友申请的详细信息
- 实时监听新的好友申请

**关键代码**:
```javascript
// 获取好友申请列表
const friendApplicationList = await wx.$TUIKit.getFriendApplicationList();

// 过滤收到的申请
const applications = friendApplicationList.data?.applicationList?.filter(
  app => app.type === wx.TencentCloudChat.TYPES.SNS_APPLICATION_SENT_TO_ME
) || [];
```

### 3. 处理好友请求测试
**位置**: `pages/chat/chat.js` - `acceptMatch` 和 `rejectMatch` 方法

**测试步骤**:
1. 在聊天页面找到待处理的好友请求
2. 点击"接受"或"拒绝"按钮
3. 观察处理结果

**预期结果**:
- **接受**: 显示成功提示，刷新会话列表，移除待处理请求
- **拒绝**:显示成功提示，移除待处理请求

**关键代码**:
```javascript
// 接受好友请求
wx.$TUIKit.acceptFriendApplication({
  type: wx.TencentCloudChat.TYPES.SNS_ACCEPT_TYPE_AGREE_AND_ADD,
  userID: userId
});

// 拒绝好友请求
wx.$TUIKit.refuseFriendApplication({
  userID: userId
});
```

### 4. 实时监听测试
**位置**: `pages/chat/chat.js` - `setupFriendApplicationListener` 方法

**测试步骤**:
1. 保持聊天页面打开
2. 让其他用户发送好友请求
3. 观察页面是否实时更新

**预期结果**:
- 收到新的好友申请时显示通知
- 好友申请被处理时自动刷新列表
- 好友列表更新时自动刷新会话列表

## 🔧 错误处理

### 常见错误码及处理
- **10009**: 已经是好友关系 → 直接跳转聊天
- **10010**: 好友申请已发送 → 提示等待对方同意
- **20009**: 非好友无法发送消息 → 提示需要先加好友
- **30001**: 服务器内部错误 → 提示稍后重试
- **50001**: 网络连接失败 → 提示检查网络

### 备用方案
当IM API失败时，会自动调用后端API作为备用方案：
```javascript
this.sendFriendRequestViaBackend(targetUser, message);
```

## 🧪 测试工具

### 控制台测试
在微信开发者工具控制台中执行：
```javascript
// 加载测试脚本
// 将test-friend-flow.js内容复制到控制台执行

// 运行测试
testFriendRequestFlow();
```

### 手动测试检查点
1. ✅ TUIKit初始化状态
2. ✅ 用户登录状态
3. ✅ 好友申请列表获取
4. ✅ 发送好友请求
5. ✅ 监听器设置
6. ✅ 错误处理机制

## 📋 测试完成清单

- [ ] preview页面发送好友请求功能正常
- [ ] chat页面能正确显示好友申请列表
- [ ] 接受好友请求功能正常
- [ ] 拒绝好友请求功能正常
- [ ] 实时监听好友状态变化正常
- [ ] 错误处理机制完善
- [ ] 备用方案（后端API）可用
- [ ] 用户体验友好（提示信息清晰）

## 🎉 预期效果

实现完整的社交功能闭环：
1. 用户A在preview页面向用户B发送好友请求
2. 用户B在chat页面收到好友请求通知
3. 用户B可以选择接受或拒绝好友请求
4. 接受后双方成为好友，可以正常聊天
5. 整个过程有实时反馈和错误处理

## 📝 注意事项

1. 确保IM控制台已开启"发送单聊消息检查关系链"配置
2. 测试前确保TUIKit已正确初始化并登录
3. 测试过程中注意查看控制台日志输出
4. 如遇问题，检查网络连接和IM服务状态