1. **处理函数设计**：

   * 函数名：`formatMatchDegreeContent`

   * 输入：原始HTML内容字符串

   * 输出：处理后的HTML内容字符串

2. **HTML解析与处理逻辑**：

   * 使用正则表达式匹配包含"总体匹配度"和"人格契合度"的文本段落

   * 识别数值部分（如：0.85 -> 85%）

   * 使用HTML标签包裹百分比数值并添加样式

3. **具体实现步骤**：

   * 第一步：匹配"总体匹配度"相关文本，如：`总体匹配度：([\d.]+)`

   * 第二步：将匹配到的数值乘以100并添加百分号，如：`0.85` -> `85%`

   * 第三步：使用`<span style="color: purple;">`包裹百分比数值

   * 第四步：匹配"人格契合度"相关文本，如：`人格契合度：([\d.]+)`

   * 第五步：同样将数值转换为百分比格式

   * 第六步：使用`<span style="color: blue;">`包裹百分比数值

4. **正则表达式设计**：

   * 总体匹配度：`/(总体匹配度：)([\d.]+)/g`

   * 人格契合度：`/(人格契合度：)([\d.]+)/g`

5. **样式定义**：

   * 总体匹配度：紫色（#800080 或 CSS color: purple）

   * 人格契合度：蓝色（#0000FF 或 CSS color: blue）

6. **调用位置**：

   * 在第261行`accumulatedContent = res.data.content;`之后调用

   * 同样在其他accumulatedContent赋值处调用（如第257行和第251行）

7. **测试考虑**：

   * 确保处理函数能正确处理不同格式的数值（整数、小数）

   * 确保处理后的HTML格式正确，无语法错误

   * 确保在没有匹配到对应文本时不会影响原有内容

8. **代码实现**：

   ```javascript
   formatMatchDegreeContent(content) {
     // 处理总体匹配度
     content = content.replace(/(总体匹配度：)([\d.]+)/g, (match, label, value) => {
       const percentage = (parseFloat(value) * 100).toFixed(0) + '%';
       return `${label}<span style="color: purple;">${percentage}</span>`;
     });
     
     // 处理人格契合度
     content = content.replace(/(人格契合度：)([\d.]+)/g, (match, label, value) => {
       const percentage = (parseFloat(value) * 100).
   ```

